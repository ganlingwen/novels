# 小说创作协作规范

`outline.md` 可以使用简洁的事件记录。正文必须重新建造成可见、可听、可触的现场，不得直接扩写提纲语言。

## 文件职责

- `outline.md`：背景、经济规则、时间线、前史、章节事件、固定钩子和主画面。
- `power.md`：共享能级、能力类别、进化方式、治疗规则、反噬机制和战力序列的唯一事实源。
- `characters.md`：人物形象、能力边界、经历、欲望、行为方式和对白基调的唯一事实源。
- `factions.md`：势力地理、制度、资源、军力、权力结构和对外关系的唯一事实源。
- `skills/write.md`：正文生成规范。
- `skills/scene.md`：分章场景事实表的构建与校验规范。
- `skills/score.md`：正文评分规范。
- `generated/chapterN_scene.md`：第 N 章的空间、机关、调度和连续性事实。
- `generated/chapterN.md`：第 N 章正文。

稳定设定只写入对应事实源，不复制到其他文件。提纲只记录稳定设定在具体章节中的状态、用途和后果。

## 任务路由

### 生成或改写正文

1. 完整读取 `skills/write.md`。
2. 在 `outline.md` 中读取目标章、相邻章节要求、固定钩子、主画面和所属幕情绪曲线。
3. 完整读取本章出场人物及被谈论人物的卡片，以及涉及势力的卡片。
4. 涉及异能、丧尸或异化动植物时读取 `power.md`；再读取本章涉及的时间、数值和地理规则。如有 `generated/chapterN_scene.md`，同时读取。
5. 已有正文时，读取目标章现稿和上一章结尾；续写时同时核对伤势、物件、人数、时间和站位。

### 生成或清理场景事实表

1. 完整读取 `skills/scene.md`。
2. 读取目标章、相邻提纲及相关人物、势力和硬规则；涉及能力、感染或异化生物时读取 `power.md`。
3. 有正文时只提取已经落地的连续状态，不用旧正文覆盖更新后的设定。

### 评分正文

1. 完整读取 `skills/score.md`。
2. 按其要求读取正文及相关事实源；不额外套用正文生成规范。

### 修改设定

- 修改提纲时核对相关人物、势力、数值和现有正文。
- 修改力量、人物或势力设定时，搜索其他事实源与现有正文中的冲突引用。
- 发现冲突先修正对应事实源，再同步受影响的场景表或正文。

## 冲突归属

当前用户明确要求优先于项目文档。其余冲突按内容归属处理：

- 行文方法：`skills/write.md`。
- 共享能级、类别、进化、治疗与反噬规则：`power.md`。
- 人物稳定设定与对白基调：`characters.md`。
- 势力稳定设定与地理：`factions.md`。
- 章级事件、时间、数值、固定钩子和主画面：`outline.md`。
- 章内空间、机关和调度细节：`generated/chapterN_scene.md`；不得覆盖以上事实源。
- 已有正文只代表已经落地的连续状态，不得覆盖更新后的事实源。

不得在正文中自行折中冲突。重点核对姓名、能力边界、账目、时间窗口、伤势、道具数量和路线方向。
